{
  "slug": "2026-04-17-fe0e82b8-article-1",
  "title": "100% 有意义的覆盖率：我如何用 OPC Loop 重新定义「测好」",
  "summary": "写测试不难，写「有意义的」测试很难。这篇文章记录了我用 OPC 多角色评审循环，从「覆盖率数字」到「覆盖率质量」的完整转变——以及为什么 95% 的 coverage 可能比 100% 更诚实。",
  "body": "# 100% 有意义的覆盖率：我如何用 OPC Loop 重新定义「测好」\n\n## 开始：一个听起来很合理的需求\n\n「我要 100% 代码覆盖率，但我需要 100% **有意义的**代码覆盖率，不要为了测试而测试。」\n\n这是我发给 OPC Loop 的第一条指令。当时我觉得这个要求很清晰——不就是写好测试吗？\n\n我错了。这句话里藏着一个真正困难的哲学问题：什么叫「有意义」？\n\n## 两种覆盖率的战争\n\n机械覆盖率（mechanical coverage）很好写。你只需要确保每行代码都被执行过一次。比如：\n\n```typescript\n// 这个测试「覆盖」了 deletePost，但什么都没测\nit('deletePost exists', () => {\n  expect(AdminService.deletePost).toBeDefined();\n});\n```\n\n这行测试会让你的覆盖率计数器加 1，但它不能告诉你 `deletePost` 在 RLS 权限不够时会怎样，在 Supabase 断线时会怎样，在 `postId` 是空字符串时会怎样。\n\n有意义的覆盖率（meaningful coverage）问的是另一个问题：**这个测试，如果失败了，我们会知道生产环境出了什么问题吗？**\n\nOPC 的 tester 角色在第一轮 review 里给出了这个判断标准，我当时觉得有点抽象。但写完所有测试之后，我完全同意了。\n\n## OPC Loop 是怎么工作的\n\n`/opc loop` 的自动模式会把任务分解成多个 unit，对每个 unit 走完整的 build → code-review → test-verify → gate 流程。每个 gate 由代码（不是 LLM）机械地聚合评分：有任何 🔴 就 FAIL，有 🟡 就 ITERATE，全绿才 PASS。\n\n对测试这个任务，OPC 拆成了三层：\n\n**Layer 1: Unit tests for services**\n\n主要是 `database.service.ts` 和 `admin.service.ts`。这两个文件直接调用 Supabase，mock 策略很关键。最终采用的是 chainable query builder 模式：\n\n```typescript\nexport function createQueryBuilder(overrides = {}) {\n  const builder: any = {\n    select: vi.fn().mockReturnThis(),\n    insert: vi.fn().mockReturnThis(),\n    eq: vi.fn().mockReturnThis(),\n    is: vi.fn().mockReturnThis(),  // 后来加的：soft-delete 需要\n    single: vi.fn().mockResolvedValue({ data: null, error: null }),\n  };\n  // 让 builder 本身可以被 await，不强制加 .single()\n  Object.defineProperty(builder, 'then', {\n    value: (onFulfilled: any, onRejected?: any) =>\n      Promise.resolve({ data: null, error: null }).then(onFulfilled, onRejected),\n  });\n  return builder;\n}\n```\n\n这让测试可以精确断言调用链：\n\n```typescript\nit('filters out deleted posts', async () => {\n  const mockBuilder = createQueryBuilder({ data: [{ id: '1', content: 'test' }], error: null });\n  supabaseMock.from.mockReturnValue(mockBuilder);\n\n  await DatabaseService.getPostsByPurpose('need_help');\n\n  expect(mockBuilder.is).toHaveBeenCalledWith('deleted_at', null);\n});\n```\n\n这个测试有意义：如果有人不小心把 `.is('deleted_at', null)` 删了，测试立刻变红。它保护的是一个真实的产品行为——公开页面不能显示已软删除的帖子。\n\n**Layer 2: 接缝识别**\n\nOPC 的 backend reviewer 提出了一个框架：只 mock「你控制不了的东西」，不 mock 内部逻辑。\n\n- Supabase 客户端 → mock（外部网络）\n- `crypto.getRandomValues` → mock（系统 API，不稳定）\n- `i18next` → mock（外部依赖）\n- `AdminService.isAuthenticatedVerified()` 内部的 `getUser()` 逻辑 → **不 mock**，让它真正走完逻辑\n\n这个原则帮我删掉了大约 30% 的冗余测试——那些测试在 mock「过程」而不是「边界」，所以测的其实是 mock 本身。\n\n**Layer 3: E2E smoke tests**\n\nPlaywright 负责关键路径：提交帖子、用 access code 找回、发回复、标记解决。目标不是 100% UI coverage，而是「关键路径不回归」。\n\n```javascript\ntest('submit and retrieve via access code', async ({ page }) => {\n  await page.goto('http://localhost:3000');\n  await page.fill('[name=\"content\"]', 'I need help with math');\n  await page.click('button[type=\"submit\"]');\n  const code = await page.textContent('.access-code');\n\n  await page.goto(`http://localhost:3000/#/past-questions?code=${code}`);\n  await expect(page.locator('.post-content')).toContainText('I need help with math');\n});\n```\n\n## 「Meaningfulness A+, Numbers: A+」\n\nOPC 最终 gate 的评分让我记到现在：\n\n- **Meaningfulness（有意义性）: A+** — 每个测试都在保护真实的产品行为\n- **Numbers（数字）: A+** — 覆盖率数字也过关\n- **但 100% 不是目标** — 有几个 utility 函数（`simpleFingerprint` 的哈希路径、`generateAccessCode` 的 fallback 分支）是基础设施代码，测它们的维护成本高于收益\n\n这个结论反直觉但正确：**95% 的高质量测试，比 100% 的低质量测试更安全。** 低质量的测试会给你虚假的安全感，而高质量的测试会在你真正出问题前告诉你。\n\n## 我学到的三件事\n\n**1. 测试的价值 = 失败时你学到多少**\n\n一个从不失败的测试，要么是在测不可能出错的东西，要么是写错了。写测试之前先问：「这个测试如果红了，意味着生产环境出了什么？」如果答不上来，别写。\n\n**2. Mock 是一把双刃剑**\n\nMock 太少，测试跑不动；mock 太多，测试变成「测 mock 本身」。只 mock 你控制不了的边界（网络、随机数、时间），不 mock 内部逻辑的过程。\n\n**3. OPC 的多角色 review 解决了「自我验证」的问题**\n\n单用 implementer 视角写测试，你会默认「我知道这段代码是对的，所以我写测试来证明它是对的」。加上 tester 和 security 视角，你会开始问「这段代码在什么情况下会出错？」——这才是测试该问的问题。两个视角不能合并，必须分开。",
  "heroImage": "",
  "tags": [
    "testing",
    "opc",
    "coverage",
    "vitest",
    "playwright"
  ],
  "sessionId": "fe0e82b8-c5a0-426b-937e-46c339b79e4e",
  "chunkIndices": [
    1,
    2,
    3,
    5,
    6,
    7,
    8,
    14,
    15,
    16,
    17
  ],
  "project": "ProblemSolver",
  "date": "2026-04-17",
  "duration": "",
  "stats": {}
}